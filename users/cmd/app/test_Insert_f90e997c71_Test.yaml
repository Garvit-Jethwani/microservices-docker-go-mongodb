# Test generated by RoostGPT for test microservice-golang using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  payload:
    path: "test_Insert_f90e997c71_Test.csv"
    fields:
      - "username"
      - "email"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/"
          json:
            username: "{{ username }}"
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "insertResult"
            - hasProperty: "insertResult.InsertedID"
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid-url"
          json:
            username: "{{ username }}"
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "err"
  - name: "API request with edge case"
    flow:
      - post:
          url: "/"
          json:
            username: "{{ username }}"
            email: "{{ email }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "insertResult"
            - hasProperty: "insertResult.InsertedID"
