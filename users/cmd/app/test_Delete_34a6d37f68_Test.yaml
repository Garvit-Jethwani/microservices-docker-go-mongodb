# Test generated by RoostGPT for test microservice-golang using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "test_Delete_34a6d37f68_Test.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: "DeletedCount"
            - hasHeader: "content-type"
            
  - name: "API request with validation failure"
    flow:
      - delete:
          url: "/invalid-id"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - delete:
          url: "/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: "DeletedCount"
            - hasHeader: "content-type"
