# Test generated by RoostGPT for test microservice-golang using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "test_All_c38f98e82e_Test.csv"
    fields: ["field1", "field2"]
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get: 
          url: "/"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - hasHeader: "Content-Type"

  - name: "API request with validation failure"
    flow:
      - get: 
          url: "/invalid"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - get: 
          url: "/"
          headers:
            Content-Type: "application/json"
            qs:
              field1: "largeData"
          expect:
            - statusCode: 200
            - hasProperty: "data"
            - hasHeader: "Content-Type"
