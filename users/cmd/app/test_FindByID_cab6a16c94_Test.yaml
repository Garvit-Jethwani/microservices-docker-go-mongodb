# Test generated by RoostGPT for test microservice-go-api using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  plugins:
    expect: {}
  payload:
    path: "test_FindByID_cab6a16c94_Test.csv"
    fields:
      - "id"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid-id"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "id"
            - hasProperty: "name"
            - hasProperty: "email"
            - hasHeader: "Content-Type"
