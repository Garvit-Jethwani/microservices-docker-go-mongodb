# Test generated by RoostGPT for test microservice-go-api using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "test_ShowtimesView_2f61551509_Test.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ id }}"
          expect:
            - statusCode: 200
            - hasProperty: "ShowTime"
            - hasProperty: "Movies"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid_id"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ id }}"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "ShowTime"
            - hasProperty: "Movies"
