# Test generated by RoostGPT for test microservice-go-api using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "test_MoviesView_15c1f36d02_Test.csv"
    fields:
      - "movieID"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ movieID }}"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 200
          - hasProperty: "Movie"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidID"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ movieID }}"
          headers:
            Content-Type: "application/json"
        expect:
          - statusCode: 200
          - hasProperty: "Movie"
          - hasProperty: "movieID"
