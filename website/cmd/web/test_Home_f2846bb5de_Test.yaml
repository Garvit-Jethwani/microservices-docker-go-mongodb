# Test generated by RoostGPT for test microservice-go-api using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "test_Home_f2846bb5de_Test.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "header"
            - hasProperty: "body"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid-url"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400
            - hasProperty: "header"
            - hasProperty: "body"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ field1 }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ field2 }}"
            - hasProperty: "header"
            - hasProperty: "body"
