# Test generated by RoostGPT for test microservice-go-api using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "test_MoviesList_61b677d252_Test.csv"
    fields:
      - "movieId"
      - "movieName"
    skipHeader: true
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - post: "/"
        headers:
          Content-Type: "application/json"
        json:
          movieId: "{{ movieId }}"
          movieName: "{{ movieName }}"
        expect:
          - statusCode: 200
          - hasProperty: "Movies"
          - hasHeader: "Content-Type"
  - name: "API request with validation failure"
    flow:
      - post: "/invalid"
        headers:
          Content-Type: "application/json"
        json:
          movieId: "{{ movieId }}"
          movieName: "{{ movieName }}"
        expect:
          - statusCode: 400
  - name: "API request with edge case"
    flow:
      - post: "/"
        headers:
          Content-Type: "application/json"
        json:
          movieId: "{{ movieId }}"
          movieName: "{{ movieName }}"
        expect:
          - statusCode:
              min: 200
              max: 500
          - hasProperty: "Movies"
          - hasHeader: "Content-Type"
