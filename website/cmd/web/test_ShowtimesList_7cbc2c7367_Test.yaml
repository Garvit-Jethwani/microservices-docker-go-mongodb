# Test generated by RoostGPT for test microservice-go-api using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "test_ShowtimesList_7cbc2c7367_Test.csv"
    fields:
      - "showtime"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/"
          headers:
            Content-Type: "application/json"
          capture:
            json: "$"
            as: "response"
      - think: 1
      - log: "Response: {{ response }}"
      - think: 1
      - assert:
          - statusCode: 200
          - hasProperty: "ShowTimes"
          - bodyContains:
              - "ShowTimes"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid"
          headers:
            Content-Type: "application/json"
          capture:
            json: "$"
            as: "response"
      - think: 1
      - log: "Response: {{ response }}"
      - think: 1
      - assert:
          - statusCode: 400
          - bodyContains:
              - "error"
              - "Invalid URL"

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ showtime }}"
          headers:
            Content-Type: "application/json"
          capture:
            json: "$"
            as: "response"
      - think: 1
      - log: "Response: {{ response }}"
      - think: 1
      - assert:
          - statusCode: {
              "in": [200, 202, 204]
            }
          - hasProperty: "ShowTimes"
          - bodyContains:
              - "ShowTimes"
