# Test generated by RoostGPT for test microservice-go-api using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "test_UsersView_e6cd794ab8_Test.csv"
    fields: ["id"]
    skipHeader: true
  plugins:
    expect: {}
  
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 200
            - hasProperty: "User"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid-url"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/{{ id }}"
          headers:
            Content-Type: "application/json"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "User"
